export type taskType = {
	task: string;
	note: string;
	duedate: string;
	priority: string;
};

export type UserDataTypeServer = {
	fullname: string;
	email: string;
	password: string;
	cpassword: string;
}